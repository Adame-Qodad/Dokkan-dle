{% extends 'base.html.twig' %}

{% block title %}{{ t.welcome_title|default('Accueil') }} - Dokkan'dle{% endblock %}

{% block body %}
    <!-- Hero Section -->
    <div class="hero-section">
        <div class="hero-content">
            <div class="hero-text">
                <h1 class="hero-title">{{ t.welcome_title|default('Bienvenue sur Dokkan\'dle') }}</h1>
                <p class="hero-subtitle">{{ t.welcome_subtitle|default('Découvrez tous les personnages de Dragon Ball Z: Dokkan Battle') }}</p>
                <div class="hero-stats">
                    <div class="stat">
                        <i class="fas fa-users"></i>
                        <span class="stat-number">1000+</span>
                        <span class="stat-label">Personnages</span>
                    </div>
                    <div class="stat">
                        <i class="fas fa-star"></i>
                        <span class="stat-number">6</span>
                        <span class="stat-label">Raretés</span>
                    </div>
                    <div class="stat">
                        <i class="fas fa-palette"></i>
                        <span class="stat-number">5</span>
                        <span class="stat-label">Types</span>
                    </div>
                </div>
            </div>
            <div class="hero-visual">
                <div class="floating-cards">
                    <div class="floating-card card-1">
                        <img src="https://static.wikia.nocookie.net/dbz-dokkanbattle/images/7/71/Card_1011680_thumb_apng.png" alt="Vegito">
                    </div>
                    <div class="floating-card card-2">
                        <img src="https://static.wikia.nocookie.net/dbz-dokkanbattle/images/c/c9/Card_1014210_thumb_apng.png" alt="Goku SS4">
                    </div>
                    <div class="floating-card card-3">
                        <img src="https://static.wikia.nocookie.net/dbz-dokkanbattle/images/2/2a/Card_1014050_thumb_apng.png" alt="Trunks">
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Features Section -->
    <div class="features-section">
        <div class="section-header">
            <h2>Explorez l'univers Dragon Ball</h2>
            <p>Découvrez toutes les fonctionnalités de Dokkan'dle</p>
        </div>
        
        <div class="features-grid">
            <div class="feature-card card">
                <div class="feature-icon">
                    <i class="fas fa-search"></i>
                </div>
                <h3>{{ t.feature_list_title|default('📋 Liste des personnages') }}</h3>
                <p>{{ t.feature_list_desc|default('Parcourez notre base de données complète de personnages avec filtres et recherche avancée.') }}</p>
                <div class="feature-highlights">
                    <span class="highlight">🔍 Recherche avancée</span>
                    <span class="highlight">🎯 Filtres multiples</span>
                    <span class="highlight">📊 Statistiques détaillées</span>
                </div>
                <a href="{{ path('units_list') }}" class="btn">
                    <i class="fas fa-arrow-right"></i>
                    {{ t.feature_list_btn|default('Voir la liste') }}
                </a>
            </div>

            <div class="feature-card card">
                <div class="feature-icon">
                    <i class="fas fa-dice"></i>
                </div>
                <h3>{{ t.feature_daily_title|default('🎲 Personnage du jour') }}</h3>
                <p>{{ t.feature_daily_desc|default('Découvrez un personnage aléatoire chaque jour pour enrichir votre collection.') }}</p>
                <div class="feature-highlights">
                    <span class="highlight">🎲 Aléatoire quotidien</span>
                    <span class="highlight">⭐ Découvertes</span>
                    <span class="highlight">📈 Collection</span>
                </div>
                <a href="{{ path('unit_of_the_day') }}" class="btn">
                    <i class="fas fa-arrow-right"></i>
                    {{ t.feature_daily_btn|default('Découvrir') }}
                </a>
            </div>

            <div class="feature-card card">
                <div class="feature-icon">
                    <i class="fas fa-globe"></i>
                </div>
                <h3>🌍 Multilingue</h3>
                <p>Profitez de Dokkan'dle dans votre langue préférée avec notre système de traduction complet.</p>
                <div class="feature-highlights">
                    <span class="highlight">🇫🇷 Français</span>
                    <span class="highlight">🇺🇸 Anglais</span>
                    <span class="highlight">🔄 Basculement facile</span>
                </div>
                <div class="btn disabled">
                    <i class="fas fa-check"></i>
                    Disponible
                </div>
            </div>
        </div>
    </div>

    <!-- Stats Section -->
    <div class="stats-section">
        <div class="stats-grid">
            <div class="stat-card card">
                <div class="stat-icon">
                    <i class="fas fa-fire"></i>
                </div>
                <div class="stat-content">
                    <h3>LR</h3>
                    <p>Personnages Légendaires</p>
                    <span class="stat-count">150+</span>
                </div>
            </div>
            
            <div class="stat-card card">
                <div class="stat-icon">
                    <i class="fas fa-crown"></i>
                </div>
                <div class="stat-content">
                    <h3>UR</h3>
                    <p>Personnages Ultra Rares</p>
                    <span class="stat-count">300+</span>
                </div>
            </div>
            
            <div class="stat-card card">
                <div class="stat-icon">
                    <i class="fas fa-gem"></i>
                </div>
                <div class="stat-content">
                    <h3>SSR</h3>
                    <p>Personnages Super Rares</p>
                    <span class="stat-count">500+</span>
                </div>
            </div>
        </div>
    </div>

    <style>
        /* Hero Section */
        .hero-section {
            min-height: 80vh;
            display: flex;
            align-items: center;
            position: relative;
            margin: -2rem -2rem 4rem -2rem;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, rgba(255, 107, 53, 0.1) 0%, rgba(247, 147, 30, 0.1) 100%);
            border-radius: 0 0 50px 50px;
        }

        .hero-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .hero-title {
            font-family: 'Orbitron', monospace;
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 1rem;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.2;
        }

        .hero-subtitle {
            font-size: 1.3rem;
            color: var(--text-gray);
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .hero-stats {
            display: flex;
            gap: 2rem;
            margin-top: 2rem;
        }

        .stat {
            text-align: center;
            padding: 1rem;
            background: rgba(255, 107, 53, 0.1);
            border-radius: 15px;
            border: 1px solid rgba(255, 107, 53, 0.2);
        }

        .stat i {
            font-size: 2rem;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .stat-number {
            display: block;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--text-light);
        }

        .stat-label {
            font-size: 0.9rem;
            color: var(--text-gray);
        }

        /* Floating Cards */
        .floating-cards {
            position: relative;
            height: 400px;
        }

        .floating-card {
            position: absolute;
            width: 120px;
            height: 180px;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: var(--shadow);
            animation: float 6s ease-in-out infinite;
        }

        .floating-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .card-1 {
            top: 20%;
            left: 20%;
            animation-delay: 0s;
        }

        .card-2 {
            top: 40%;
            right: 30%;
            animation-delay: 2s;
        }

        .card-3 {
            bottom: 20%;
            left: 40%;
            animation-delay: 4s;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
        }

        /* Features Section */
        .features-section {
            margin: 4rem 0;
        }

        .section-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .section-header h2 {
            font-family: 'Orbitron', monospace;
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-header p {
            font-size: 1.2rem;
            color: var(--text-gray);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            text-align: center;
            padding: 2.5rem;
        }

        .feature-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 1.5rem;
            background: var(--gradient-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: var(--text-light);
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--text-light);
        }

        .feature-card p {
            color: var(--text-gray);
            margin-bottom: 1.5rem;
            line-height: 1.6;
        }

        .feature-highlights {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            justify-content: center;
            margin-bottom: 2rem;
        }

        .highlight {
            background: rgba(255, 107, 53, 0.1);
            color: var(--primary-color);
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .btn.disabled {
            background: var(--gradient-secondary);
            cursor: default;
        }

        /* Stats Section */
        .stats-section {
            margin: 4rem 0;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .stat-card {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            padding: 2rem;
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            background: var(--gradient-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: var(--text-light);
        }

        .stat-content h3 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .stat-content p {
            color: var(--text-gray);
            margin-bottom: 0.5rem;
        }

        .stat-count {
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-light);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .hero-title {
                font-size: 2.5rem;
            }

            .hero-stats {
                justify-content: center;
            }

            .floating-cards {
                height: 300px;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
{% endblock %} 